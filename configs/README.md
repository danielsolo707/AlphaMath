# Configs

| File | Role |
|------|------|
| `default.yaml` | Local real-model defaults |
| `kaggle.yaml` | Kaggle / T4-oriented profile |
| `smoke_mock.yaml` | Fast CPU mock pipeline (plumbing only) |

Paths inside YAML are relative to the **repo root** (e.g. `data/sample_problems.json`).
