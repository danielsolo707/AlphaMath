# Tests

```bash
python -m unittest discover -s tests -v
# or
pip install -r requirements/dev.txt
pytest
```

Covers agent voting/repair, sandbox isolation, reporting, packaging, and submission resume.
