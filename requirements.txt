# Core agent (CPU ok)
sympy>=1.12
numpy>=1.24
pydantic>=2.0
pyyaml>=6.0
tqdm>=4.66
rich>=13.0
httpx>=0.27

# Open-weight math model (DeepSeek-Math) — needed for real / Kaggle inference
torch>=2.1.0
transformers>=4.40.0
accelerate>=0.28.0
sentencepiece>=0.1.99
protobuf>=4.25.0
huggingface_hub>=0.22.0

# 4-bit quantization for Kaggle T4 16GB (Linux/CUDA). Optional on Windows CPU demos.
# bitsandbytes>=0.43.0
